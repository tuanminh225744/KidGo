version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: kidgo_backend
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - mongo
      - redis
    volumes:
      - ./backend:/app
      - /app/node_modules

  # frontend:
  #   build:
  #     context: ./frontend
  #   container_name: kidgo_frontend
  #   restart: always
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - backend

  mongo:
    image: mongo:7
    container_name: kidgo_mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    container_name: kidgo_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  mongo_data:
  redis_data:
